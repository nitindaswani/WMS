[build]
  base = "Frontend"
  publish = "."
  command = "echo 'No build command needed for static site'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false # Only when file not found (history mode fallback, optional for multi-page but safe)
